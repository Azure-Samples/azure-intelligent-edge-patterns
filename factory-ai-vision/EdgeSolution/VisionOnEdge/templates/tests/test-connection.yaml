apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "VisionOnEdge.fullname" . }}-test-connection"
  labels:
    {{- include "VisionOnEdge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "VisionOnEdge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
